---
- name: Test Ansible automation on worker nodes
  hosts: workers
  become: true
  gather_facts: true

  pre_tasks:
    - name: Update package cache
      ansible.builtin.apt:
        update_cache: true
        cache_valid_time: 3600
        lock_timeout: "{{ apt_lock_timeout | default(300) }}"
      register: test_apt_cache
      until: test_apt_cache is succeeded
      retries: 3
      delay: 10
      when: ansible_os_family == "Debian"

    - name: Ensure required directories exist
      ansible.builtin.file:
        path: "{{ item }}"
        state: directory
        mode: "0755"
      loop:
        - /opt/cni/bin
        - /etc/wireguard
        - /etc/kubernetes

  roles:
    - common

  post_tasks:
    - name: Verify CNI plugins installation
      ansible.builtin.command: ls /opt/cni/bin/loopback
      register: cni_check
      changed_when: false

    - name: Display installation success
      ansible.builtin.debug:
        msg: "CNI plugins successfully installed: {{ cni_check.stdout_lines }}"
      when: cni_check.rc == 0
